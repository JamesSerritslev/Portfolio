import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        gold: "#9F956C",
        teal: "#153243",
        "gold-border": "rgb(159 149 108 / 0.2)",
      },
    },
  },
};

export default config;
